export default function SeoSetup() {
  return (
    <div style={{ padding: 24, fontFamily: 'system-ui' }}>
      <h1>SEO Checklist Setup</h1>
      <p>If you can see this, routing works. Next we can enable the seed button.</p>
    </div>
  )
}
